{
    "name": "rover",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "test": "vitest",
        "format": "prettier . --write",
        "check": "prettier . --check"
    },
    "devDependencies": {
        "prettier": "3.0.3",
        "typescript": "^5.0.2",
        "vite": "^4.4.5",
        "vitest": "^0.34.3"
    }
}
